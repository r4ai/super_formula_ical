[project]
name = "super-formula-ical"
version = "0.1.0"
description = "Generate ICS calendars from SUPER FORMULA race schedules."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
